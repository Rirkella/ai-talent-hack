[pytest]
testpaths = tests
markers =
    smoke: проверки окружения и связки с LLM (нужен запущенный провайдер)
    llm: тесты, реально вызывающие модель — медленные
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
